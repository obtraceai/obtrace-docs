---
title: How AI Decides to Open a PR
description: Confidence thresholds, guardrails, and repository permission checks that control remediation pull requests.
category: concept
intent: explanation
llm_priority: high
sourceOfTruth: true
---

# How AI Decides to Open a PR

Obtrace does not open pull requests based on anomaly detection alone. The system requires a coherent root cause hypothesis, enough evidence, and a repository policy that permits the action.

## Decision inputs

- Incident confidence
- Root cause confidence
- Availability of a narrow remediation path
- Repository and branch permissions
- Approval policy for the environment

## Decision flow

```mermaid
flowchart TD
  A["Incident detected\nhigh-confidence signal"]:::input --> B["Root cause scored\nLLM + telemetry evidence"]:::process
  B --> C{"Policy check\nconfidence ≥ threshold?"}:::decision
  C -->|Yes| D["Draft remediation plan\nnarrow, safe change"]:::process
  C -->|No| STOP["Skip PR\nlog reason"]:::reject
  D --> E{"Repo permissions\nbranch + app access?"}:::decision
  E -->|Yes| PR["Open PR\nwith evidence + test plan"]:::output
  E -->|No| MANUAL["Flag for manual review"]:::reject

  classDef input fill:#2563eb,stroke:#1d4ed8,color:#fff,stroke-width:2px
  classDef process fill:#7c3aed,stroke:#6d28d9,color:#fff,stroke-width:2px
  classDef decision fill:#d97706,stroke:#b45309,color:#fff,stroke-width:2px
  classDef output fill:#059669,stroke:#047857,color:#fff,stroke-width:2px
  classDef reject fill:#dc2626,stroke:#b91c1c,color:#fff,stroke-width:2px
```

## When Obtrace should not open a PR

- Confidence is below policy threshold
- The blast radius is unclear
- The repository app lacks permission
- The change is destructive or high-risk
